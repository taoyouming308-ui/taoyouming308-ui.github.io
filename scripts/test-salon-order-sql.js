const fs=require('fs'),sql=fs.readFileSync('supabase/migrations/20260906072125_salon_order_workflow.sql','utf8'),bad=[];const ok=(x,m)=>{if(!x)bad.push(m)};
['salon_create_order','salon_replace_order_lines','salon_set_order_status','salon_get_order','salon_order_number_seq'].forEach(x=>ok(sql.includes(x),x+' missing'));ok(/orders','write/.test(sql)&&/orders','discount/.test(sql)&&/orders','read/.test(sql),'order permissions missing');ok(/只有草稿订单可以修改明细/.test(sql),'draft mutation guard missing');ok(/for update/g.test(sql),'order lock missing');ok(/item_code text[\s\S]*item_name text[\s\S]*item_type text/.test(sql),'line snapshots missing');ok(!/security definer/i.test(sql),'functions must use invoker');ok((sql.match(/revoke execute on function/g)||[]).length===4,'function revokes missing');
if(bad.length){console.error('salon order SQL tests failed:\n- '+bad.join('\n- '));process.exit(1)}console.log('salon order SQL tests passed: draft editing, snapshots, discount permission, state machine, scope');

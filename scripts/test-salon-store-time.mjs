import assert from 'node:assert/strict';
import {execFileSync} from 'node:child_process';
import {instantToStoreInput,formatStoreInstant,resolveStoreTime,storeTimeToInstant} from '../packages/salon-core/store-time.mjs';
assert.equal(storeTimeToInstant('2026-09-07T00:15','Asia/Shanghai'),'2026-09-06T16:15:00.000Z');
assert.equal(instantToStoreInput('2026-09-06T16:15:00Z','Asia/Shanghai'),'2026-09-07T00:15');
assert.equal(storeTimeToInstant('2026-09-07T00:15','Asia/Kathmandu'),'2026-09-06T18:30:00.000Z');
assert.equal(storeTimeToInstant('2026-01-01T00:00','Pacific/Kiritimati'),'2025-12-31T10:00:00.000Z');
assert.equal(storeTimeToInstant('2026-01-01T00:00','Etc/GMT+12'),'2026-01-01T12:00:00.000Z');
assert.equal(resolveStoreTime('2026-03-08T02:30','America/New_York').status,'nonexistent');
assert.deepEqual(resolveStoreTime('2026-11-01T01:30','America/New_York').instants,['2026-11-01T05:30:00.000Z','2026-11-01T06:30:00.000Z']);
assert.throws(()=>storeTimeToInstant('2026-11-01T01:30','America/New_York'),e=>e.code==='AMBIGUOUS_LOCAL_TIME');
assert.throws(()=>storeTimeToInstant('2026-03-08T02:30','America/New_York'),e=>e.code==='NONEXISTENT_LOCAL_TIME');
assert.equal(resolveStoreTime('2026-04-05T01:45','Australia/Lord_Howe').instants.length,2);
assert.equal(resolveStoreTime('2026-10-04T02:15','Australia/Lord_Howe').status,'nonexistent');
assert.equal(resolveStoreTime('2011-12-30T12:00','Pacific/Apia').status,'nonexistent');
assert.equal(storeTimeToInstant('2028-02-29T10:00','UTC'),'2028-02-29T10:00:00.000Z');
for(const value of ['2026-02-29T10:00','2026-04-31T10:00','2026-01-01T24:00','2026-01-01T12:60','','2026-01-01','2026-01-01T10:00Z'])assert.throws(()=>storeTimeToInstant(value,'UTC'));
for(const zone of [undefined,null,'','Invalid/Zone',' Asia/Shanghai','+08:00'])assert.throws(()=>storeTimeToInstant('2026-01-01T10:00',zone),e=>e.code==='INVALID_TIME_ZONE');
for(const value of ['2026-01-01T10:00:00','2026-02-30T10:00:00Z','2026-01-01T10:00:60Z'])assert.throws(()=>instantToStoreInput(value,'UTC'));
assert.match(formatStoreInstant('2026-11-01T05:30:00Z','America/New_York'),/UTC-04:00/);
assert.match(formatStoreInstant('2026-11-01T06:30:00Z','America/New_York'),/UTC-05:00/);
assert.equal(formatStoreInstant('2026-09-06T16:15:22.123456Z','Asia/Shanghai'),'2026-09-07 00:15:22 (Asia/Shanghai, UTC+08:00)');
const moduleUrl=new URL('../packages/salon-core/store-time.mjs',import.meta.url).href;
const code=`import {storeTimeToInstant,instantToStoreInput} from '${moduleUrl}'; console.log(storeTimeToInstant('2026-09-07T00:15','Asia/Shanghai'),instantToStoreInput('2026-09-06T16:15:00Z','Asia/Shanghai'));`;
const results=['UTC','America/Los_Angeles','Asia/Tokyo'].map(TZ=>execFileSync(process.execPath,['--input-type=module','-e',code],{env:{...process.env,TZ},encoding:'utf8'}));
assert.ok(results.every(r=>r===results[0]));
console.log('Store time passed: cross-day, non-hour offsets, DST gap/fold, skipped day, strict inputs, explicit offset display and device-zone independence');
